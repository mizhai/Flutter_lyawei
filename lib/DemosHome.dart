import 'package:flutter/material.dart';

class DemosHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      appBar: (AppBar(
        title: Text('FlutterDemo合集'),
      )),
      body: Center(
        child: Text('HelloFlutter'),
      ),
    );
  }
}
