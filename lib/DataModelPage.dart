import 'package:flutter/material.dart';

class DataModelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      appBar: AppBar(
        title: Text('数据请求与处理'),
      ),
    );
  }
}
